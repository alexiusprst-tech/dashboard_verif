/**
 * StatusVerifikasiPage — Koordinator page for viewing verification status.
 * Source: user-flow.md Section 2.4, visionSnPRD.md FR-14
 *
 * NEEDS CONFIRMATION: filter/scope of display (user-flow Section 2.4) —
 * whether it shows only the Koordinator's own soal, or all soal for their MK.
 */

export default function StatusVerifikasiPage() {
  // TODO: Implement verification status display
  // - List of soal with their verification status
  // - Filter by semester (BR-11)

  return (
    <div>
      <h1>Status Verifikasi</h1>
      <p>Halaman melihat status verifikasi soal</p>
    </div>
  );
}
